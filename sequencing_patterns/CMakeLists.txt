if(ENABLE_DOUBLE_BUFFER)
    add_subdirectory(double_buffer)
endif()

if(ENABLE_GAME_LOOP)
    add_subdirectory(game_loop)
endif()

if(ENABLE_UPDATE_METHOD)
    add_subdirectory(update_method)
endif()


